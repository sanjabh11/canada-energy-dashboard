# Prediction Infrastructure Implementation Summary — July 2026

*Generated: 2026-07-06 | Plan: `prediction-gap-2ec00c.md` | Phases: 4/4 complete*

---

## Table 1: What Is Now Possible That Was Not Before

| # | Functionality / Improvement | Before This Thread | After This Thread | File(s) |
|---|---|---|---|---|
| 1 | **Pinball loss computation (TS)** | Only in Python `benchmark.py:105`. TS had zero implementation. | `pinballLoss(actual, forecast, quantile)` — asymmetric penalty for over/under forecasting at any quantile. | `src/lib/scoringRules.ts:3-6` |
| 2 | **Mean pinball loss over arrays** | Not available in TS. | `meanPinballLoss(actuals[], forecasts[], q)` with length validation. | `src/lib/scoringRules.ts:8-14` |
| 3 | **CRPS computation (TS)** | Python only (`benchmark.py:109`). `forecastEvaluationContract.ts` listed `crps` as a required metric but had no compute function. `regulatoryScenarios.ts` used hardcoded values. | `computeCRPS(actuals, p10, p50, p90)` — average pinball at P10/P50/P90, matching Python reference. | `src/lib/scoringRules.ts:16-22` |
| 4 | **Brier score for probabilistic events** | Not available anywhere in codebase. | `computeBrierScore(probs[], outcomes[])` — mean squared error of predicted probabilities vs binary outcomes. | `src/lib/scoringRules.ts:24-30` |
| 5 | **Empirical coverage rate measurement** | `conformalPrediction.ts` could *produce* intervals but no function to *measure* empirical coverage on held-out data. | `computeCoverageRate(actuals, lower[], upper[])` — counts fraction of actuals within bounds. | `src/lib/scoringRules.ts:32-38` |
| 6 | **MAE, RMSE, MAPE in shared module** | Referenced in contract metric names but no shared implementation. Each consumer rolled their own. | `computeMAE`, `computeRMSE`, `computeMAPE` with input validation. MAPE handles near-zero actuals safely. | `src/lib/scoringRules.ts:40-63` |
| 7 | **Skill score (model vs baseline)** | Listed as metric type in `forecastEvaluationContract.ts` but never computed. | `computeSkillScore(modelMAE, baselineMAE)` with zero-division guard. | `src/lib/scoringRules.ts:65-68` |
| 8 | **One-call forecast evaluation** | `evaluateAgainstContract()` accepted pre-computed metrics but had no way to compute them from raw arrays. | `evaluateForecast(input)` takes raw actuals + forecasts + optional quantiles/bounds → returns all metrics. | `src/lib/scoringRules.ts:70-99` |
| 9 | **Compute-and-evaluate in one call** | Two-step: compute elsewhere, then pass to contract evaluator. | `computeAndEvaluate(contract, horizon, input, modelName, modelClass)` — compute + evaluate in single function. | `src/lib/forecastEvaluationContract.ts:215-225` |
| 10 | **26 unit tests for scoring rules** | Zero tests for any scoring rule in TS. | 26 tests covering all 10 functions: edge cases (empty arrays, length mismatch, zero baseline), correctness (known values, symmetry), integration. | `tests/unit/scoringRules.test.ts:1-164` |
| 11 | **Supabase table for ACI calibration state** | No persistence layer for ACI state. `ConformalPredictor` kept state in memory only. | `conformal_calibration_state` table: alpha, target_alpha, gamma, empirical_coverage, calibration_size with RLS. | `scripts/sql/setup-conformal-calibration.sql:4-20` |
| 12 | **Supabase table for calibration observations** | No persistent record of forecast-vs-actual pairs. | `conformal_observations` table: timestamp, forecast_mw, actual_mw, lower_q, upper_q, nonconformity_score, covered with source index. | `scripts/sql/setup-conformal-calibration.sql:23-34` |
| 13 | **Live calibration Edge Function** | No automated pipeline. Calibration was manual, in-memory. | `calibration-cron` Edge Function: fetches AESO actuals, reads latest forecast, computes nonconformity, inserts observation, updates ACI alpha, computes rolling empirical coverage. | `supabase/functions/calibration-cron/index.ts:1-179` |
| 14 | **Hourly GitHub Actions cron for calibration** | No scheduled calibration job. | GitHub Actions workflow runs hourly at minute 5, POSTs to Edge Function with CRON_SECRET, fails on non-200. | `.github/workflows/cron-conformal-calibration.yml:1-26` |
| 15 | **Frontend calibration service client** | No way for React app to read calibration state or observations. | `fetchCalibrationState()` and `fetchRecentObservations()` with proper TypeScript types. | `src/lib/calibrationService.ts:1-69` |
| 16 | **TSFM benchmark results data** | `tsfmInterface.ts` defined types and `parseBenchmarkReport()` but no actual JSON data existed. | JSON with 9 results across 4 models (Chronos-2, Chronos-Bolt, Moirai-2, IBM TTM) at 3 horizons (1h, 24h, 168h). | `public/data/tsfm-benchmark-results.json:1-118` |
| 17 | **TSFM benchmark display route** | No route or page. `parseBenchmarkReport()` was never called by any component. | `/tsfm-benchmarks` route: per-horizon comparison tables, model detail cards, methodology footer. | `src/components/TsfmBenchmarkPage.tsx:1-129`, `src/App.tsx:90,154` |
| 18 | **Strategic competitor sensitivity tab** | ScenarioWorkbench had 4 tabs (Sensitivity, Uncertainty, Comparison, Export). No way to model competitor actions. | 5th "Strategic" tab: 5 competitor parameter sliders, 1000-sample Sobol QMC simulation, P5/P50/P90/Mean pipeline impact. Renders independently of `hasResults` guard. | `src/components/ScenarioWorkbenchPage.tsx:92,339-483,689,950-952` |

---

## Table 2: Per-Functionality Analysis

| # | (a) Functionality Name | (c) How Activated + Top 3 Use Cases | (d) Relationship to Existing Capabilities |
|---|---|---|---|
| 1 | **Pinball Loss** | **Activated via:** `import { pinballLoss } from './scoringRules'` — pure function, no setup needed. **Use cases:** (1) Evaluate quantile forecast accuracy at P10/P50/P90 for regulatory filings. (2) Compare model performance per-quantile to identify systematic bias (e.g., model under-predicts at P90). (3) Feed into CRPS computation as building block. | **Complementary** to `conformalPrediction.ts` which produces quantile forecasts but had no way to score them in TS. **Supplementary** to Python `benchmark.py:105` — now both languages have parity. |
| 2 | **Mean Pinball Loss** | **Activated via:** `import { meanPinballLoss } from './scoringRules'`. **Use cases:** (1) Average pinball loss across a test set for a single quantile level. (2) Compare TSFM models at median (P50) to identify best point-forecast model. (3) Input to `computeCRPS`. | **Complementary** to `pinballLoss` — aggregates single observations into dataset-level metric. **Redundant** with Python `compute_pinball_loss` by design (TS port for frontend use). |
| 3 | **CRPS** | **Activated via:** `import { computeCRPS } from './scoringRules'`. **Use cases:** (1) Primary metric for TSFM benchmark comparison (matches `TsfmBenchmarkMetrics.crps` in `tsfmInterface.ts:38`). (2) Regulatory filing evidence — OEB/CER require probabilistic forecast accuracy. (3) Conformal prediction calibration quality check. | **Complementary** to `conformalPrediction.ts` CQR pipeline — CRPS measures the quality of calibrated intervals. **Supplementary** to `forecastEvaluationContract.ts` which lists `crps` as a required metric type — now the computation exists. |
| 4 | **Brier Score** | **Activated via:** `import { computeBrierScore } from './scoringRules'`. **Use cases:** (1) Evaluate probability forecasts for binary events (e.g., "will pool price exceed $100/MWh?"). (2) Calibrate event-probability models against historical outcomes. (3) Compare probabilistic alert systems (e.g., GA ICI peak predictor). | **Complementary** to `GaIciPeakPredictorPage` — could score its peak probability predictions. **Not redundant** — no other function in the codebase computes Brier score. |
| 5 | **Coverage Rate** | **Activated via:** `import { computeCoverageRate } from './scoringRules'`. **Use cases:** (1) Verify conformal prediction intervals achieve nominal coverage (e.g., 90%). (2) Regulatory evidence that prediction intervals are well-calibrated. (3) Trigger recalibration when empirical coverage drifts from target. | **Complementary** to `conformalPrediction.ts` — `ConformalPredictor` produces intervals with coverage guarantees; this function *measures* whether those guarantees hold empirically. **Complementary** to `calibration-cron` Edge Function which records `covered` boolean per observation. |
| 6 | **MAE / RMSE / MAPE** | **Activated via:** `import { computeMAE, computeRMSE, computeMAPE } from './scoringRules'`. **Use cases:** (1) Standard point-forecast accuracy reporting for OEB/CER filings. (2) Compare TSFM models against persistence/seasonal-naive baselines. (3) Monitor forecast degradation over time in calibration pipeline. | **Complementary** to `forecastEvaluationContract.ts` which lists `mape` as a required metric — now the computation exists. **Supplementary** to hardcoded values in `regulatoryScenarios.ts:157,176,194,212,230` — those can now be computed from actual data. |
| 7 | **Skill Score** | **Activated via:** `import { computeSkillScore } from './scoringRules'`. **Use cases:** (1) Report "our model is X% better than persistence" in regulatory filings. (2) Compare TSFM models relative to seasonal-naive baseline (matches `TsfmBenchmarkMetrics.skillVsPersistence`). (3) Determine if a model adds value over a trivial baseline before deployment. | **Complementary** to `computeMAE` — requires a baseline MAE to compare against. **Supplementary** to `tsfmInterface.ts:39-40` which defines `skillVsPersistence` and `skillVsSeasonalNaive` — now the computation exists. |
| 8 | **evaluateForecast (one-call)** | **Activated via:** `import { evaluateForecast } from './scoringRules'`. **Use cases:** (1) Evaluate a forecast model on a test set with a single function call — returns all applicable metrics. (2) Feed results into `evaluateAgainstContract` for pass/fail against thresholds. (3) Quick sanity check during model development. | **Complementary** to `forecastEvaluationContract.ts:evaluateAgainstContract` — `evaluateForecast` computes the metrics, `evaluateAgainstContract` checks them against thresholds. **Superset** of individual functions 1-7. |
| 9 | **computeAndEvaluate** | **Activated via:** `import { computeAndEvaluate } from './forecastEvaluationContract'`. **Use cases:** (1) End-to-end model evaluation: raw arrays → pass/fail verdict in one call. (2) Automated CI gate: fail build if model doesn't meet contract thresholds. (3) Batch model comparison across horizons. | **Complementary** — combines `evaluateForecast` + `evaluateAgainstContract` into one step. **Not redundant** — the convenience function didn't exist before. |
| 10 | **26 Unit Tests** | **Activated via:** `npx vitest run tests/unit/scoringRules.test.ts`. **Use cases:** (1) Regression testing — ensure scoring rule implementations don't break. (2) Documentation — tests show expected inputs/outputs for each function. (3) CI gate — block merges if scoring rules fail. | **Complementary** to all functions 1-9 — provides correctness guarantees. **Supplementary** to existing `tests/unit/forecastBaselines.test.ts` — extends test coverage to scoring rules. |
| 11 | **Calibration State Table** | **Activated via:** Run `scripts/sql/setup-conformal-calibration.sql` against Supabase. **Use cases:** (1) Persist ACI alpha between cron runs — no more in-memory-only state. (2) Query current calibration state from frontend via `calibrationService.ts`. (3) Audit trail for regulatory compliance — show calibration history. | **Complementary** to `conformalPrediction.ts:ConformalPredictor` — provides persistent backing store for `aciState`. **Not redundant** — no persistence layer existed. |
| 12 | **Calibration Observations Table** | **Activated via:** Same SQL migration. **Use cases:** (1) Store each (forecast, actual, nonconformity, covered) pair for post-hoc analysis. (2) Compute rolling empirical coverage over last 100 observations. (3) Export calibration evidence for OEB/CER filings. | **Complementary** to `conformalPrediction.ts:CalibrationRecord` interface — provides persistent storage. **Complementary** to `computeCoverageRate` — stored observations can be queried and coverage computed. |
| 13 | **Calibration Cron Edge Function** | **Activated via:** Deploy to Supabase, set env vars (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `AESO_API_KEY`, `CRON_SECRET`). Trigger via GitHub Actions or manual POST. **Use cases:** (1) Hourly automated calibration: fetch AESO actuals, compare to forecasts, update ACI state. (2) Hands-off pipeline — no manual intervention needed. (3) Returns structured JSON for monitoring and alerting. | **Complementary** to `conformalPrediction.ts` ACI algorithm — implements the update rule `alpha_{t+1} = alpha_t + gamma * err` with persistence. **Complementary** to existing `aeso-ingestion-cron` Edge Function pattern — follows same auth and structure. |
| 14 | **GitHub Actions Cron** | **Activated via:** Push to `main` — workflow auto-registers. Runs hourly at minute 5. Set `SUPABASE_URL` and `CRON_SECRET` in GitHub repo secrets. **Use cases:** (1) Automated hourly calibration without Supabase Pro cron. (2) `workflow_dispatch` for manual triggering during testing. (3) GitHub Actions logs provide audit trail. | **Complementary** to Edge Function — provides the scheduling layer. **Supplementary** to existing `.github/workflows/` patterns in the repo. |
| 15 | **Calibration Service Client** | **Activated via:** `import { fetchCalibrationState, fetchRecentObservations } from './calibrationService'`. **Use cases:** (1) Display current calibration alpha and empirical coverage on a dashboard. (2) Show recent observations table for regulatory evidence. (3) Trigger UI alert when empirical coverage drifts from target. | **Complementary** to tables 11-12 — provides frontend read access. **Uses** `supabaseClient.ts` for consistent Supabase configuration. **Not yet wired** into any React component — pending integration. |
| 16 | **TSFM Benchmark JSON** | **Activated via:** Fetch from `/data/tsfm-benchmark-results.json` (served by Vite from `public/`). **Use cases:** (1) Display benchmark results without running Python. (2) Compare TSFM models at a glance for model selection. (3) Provide evidence to stakeholders that models were evaluated. | **Complementary** to `tsfmInterface.ts:parseBenchmarkReport()` — provides actual data to parse. **Supplementary** to Python `benchmark.py` — offline snapshot of results. Currently uses synthetic data; real benchmark run will replace. |
| 17 | **TSFM Benchmark Route** | **Activated via:** Navigate to `/tsfm-benchmarks` in the browser. Lazy-loaded via `React.lazy`. **Use cases:** (1) Stakeholder demonstration of TSFM model comparison. (2) Regulatory evidence page showing model evaluation methodology and results. (3) Internal model selection — compare CRPS, MAE, skill scores side-by-side. | **Complementary** to `tsfmInterface.ts` types and `parseBenchmarkReport()`. **Supplementary** to `/forecast-benchmarking` route — different focus (foundation models vs custom models). **Not redundant** — no TSFM-specific display existed. |
| 18 | **Strategic Competitor Sensitivity Tab** | **Activated via:** Navigate to `/scenario-workbench`, click "Strategic" tab (5th tab with sword icon). Renders independently — no need to run main analysis first. **Use cases:** (1) Model competitor price cut impact on pipeline (e.g., "if Orennia drops price 20%, what's our P5/P90 pipeline?"). (2) Stress-test market entry timing scenarios (e.g., "if competitor enters 12 months late, how much advantage?"). (3) Quantify switching cost as competitive moat — show P90 improvement as switching cost increases. | **Complementary** to existing `UncertaintyEngine` — reuses Sobol QMC sampling for a new domain (competitor analysis vs emissions modeling). **Supplementary** to existing 4 tabs — adds strategic/business dimension to technical scenario workbench. **Not redundant** — no competitor modeling existed. |

---

## Remaining Items

| # | Item | Status | Next Step |
|---|---|---|---|
| 1 | **Calibration service not wired to UI** | `calibrationService.ts` exists but no React component calls it | Create a `CalibrationStatusCard` component to display alpha, coverage, and recent observations |
| 2 | **TSFM benchmark JSON is synthetic** | `tsfm-benchmark-results.json` uses synthetic data | Run `benchmark.py` on real AESO data and replace JSON with real results |
| 3 | **SQL migration not applied** | `setup-conformal-calibration.sql` exists but not run against production Supabase | Run `psql -f scripts/sql/setup-conformal-calibration.sql` against production, verify RLS policies |
| 4 | **Edge Function not deployed** | `calibration-cron/index.ts` exists locally but not deployed to Supabase | `supabase functions deploy calibration-cron` after setting env vars |
| 5 | **GitHub Actions secrets not set** | `SUPABASE_URL` and `CRON_SECRET` not in GitHub repo secrets | Add secrets in GitHub repo settings → Secrets and variables → Actions |
| 6 | **Strategic tab model is linear** | `strategicPipelineModel` uses simple linear coefficients | Validate coefficients against market data or replace with learned model |
| 7 | **Plan verdicts: Chronos-2 Edge Function** | DEFERRED per plan — needs GPU infra | Research serverless GPU providers (Modal, Replicate) for inference |
| 8 | **Plan verdicts: Game-theoretic simulator** | DEFERRED per plan — high complexity, no buyer demand | Revisit if buyer demand emerges |
| 9 | **Plan verdicts: Prediction market layer** | REJECTED per plan — no precedent | Not pursuing |

---

## Verification Evidence

| Check | Command | Result |
|---|---|---|
| TypeScript build | `npx tsc -b --noEmit` | Exit 0, 0 errors |
| Scoring rules tests | `npx vitest run tests/unit/scoringRules.test.ts` | 26/26 passed |
| Regression tests | `npx vitest run tests/unit/forecastBaselines.test.ts` | 4/4 passed |
| Route registration | `grep "tsfm-benchmarks" src/App.tsx` | Line 154: route registered |
| Strategic tab wiring | `grep "activeTab === 'strategic'" src/components/ScenarioWorkbenchPage.tsx` | Line 950: renders outside hasResults guard |
