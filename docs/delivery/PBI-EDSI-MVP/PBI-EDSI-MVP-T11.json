{
  "task_id": "PBI-EDSI-MVP-T11",
  "title": "Edge helper fail-fast guard (env not configured)",
  "type": "code",
  "status": "InProgress",
  "files": [
    "src/lib/edge.ts",
    "docs/delivery/PBI-EDSI-MVP/tasks_index.json"
  ],
  "test_plan": {
    "steps": [
      "With VITE_USE_STREAMING_DATASETS=true and missing Supabase env, verify no app-initiated edge calls (manager guards).",
      "If any module calls edge helpers directly with missing env, ensure clear Error is thrown (not JSON parse).",
      "With valid env + streaming on, verify normal streaming works (no guard error)."
    ]
  },
  "status_history": []
}
